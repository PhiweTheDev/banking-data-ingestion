# banking-data-ingestion
The aim behind this repository is to isolate thee distinct ingestion axes using synthetic banking data so each project teaches one clear architectural pattern instead of blurring them. All three use the same synthetic data source. 
